[project]
name = "dili-reliability-control-plane"
version = "0.1.0"
description = "Deployment safety, reliability, cost, security, and compliance gates for an ECS/Fargate SaaS platform"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
